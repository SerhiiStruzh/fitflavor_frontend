const Loading = () => {
    return(
        <div className="flex items-center justify-center">
            <span className="font-kanit text-xl">Loading</span>
        </div>
    )
}

export default Loading;
